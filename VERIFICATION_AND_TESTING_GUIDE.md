# دليل التحقق والاختبار اليدوي — درايَة

هذا المستند يوثق منهجية التحقق من الجودة (Standard Quality Verification Workflow) المتبعة في المشروع، ودليل اختبار يدوي شامل لكل الشاشات والتدفقات (Flows) الحالية لكل من الطالب والمعلم، بالإضافة إلى رابط الاستضافة على Vercel.

آخر تحديث: 2026-08-24 — بعد تدقيق شامل لكل الـ API الحقيقي (swagger) مقابل الكود بالكامل، وإصلاح 9 أعطال حقيقية مؤكدة: سباق تحديث توكن المصادقة (سبب الخطأ المتقطع عند بدء الامتحان)، بيانات إشعارات وهمية، نسب نقاط الضعف بتظهر 0% دايماً، شارة "بحاجة اعتماد" في مراجعة المعلم مبتقفلش، حصة الامتحانات المجانية بالذكاء الاصطناعي بتتجاهَل، بيانات لوحة تحكم الطالب مش حقيقية، وتخمين مقياس الدرجة (0-5 بدل 0-100) في 4 أماكن بلوحة ودرجات المعلم. التفاصيل الكاملة في `IMPLEMENTATION_WALKTHROUGH_2026-08-24.md`.

---

## 🌐 رابط الاستضافة الحية على Vercel (Production Live Deployment)

- 🔗 **رابط الإنتاج المباشر:** [https://draya-lms.vercel.app](https://draya-lms.vercel.app)
- 📊 **رابط لوحة الفحص (Inspect):** [https://vercel.com/mahmoud1-2mostafas-projects/draya-lms](https://vercel.com/mahmoud1-2mostafas-projects/draya-lms)

> ملاحظة: الروابط أدناه معطاة كمسارات نسبية (`/student/...`, `/teacher/...`) — أضف رابط الإنتاج قبلها، أو استخدم `http://localhost:4200` عند التشغيل محلياً بـ `ng serve`.

---

## 📋 منهجية التحقق قبل أي دمج (Verification Pipeline)

يُنفَّذ هذا التسلسل بالكامل بعد أي تعديل في الكود، قبل الدمج في `develop`:

1. **Linting:**

   ```bash
   npx ng lint
   ```

   _القاعدة: صفر أخطاء وصفر تحذيرات._

2. **التنسيق (Prettier):**

   ```bash
   npx prettier --write "src/**/*.{ts,html,scss,json}"
   ```

   يُفضَّل تمرير قائمة الملفات المعدَّلة صراحةً بدل النمط الشامل لتفادي إعادة تنسيق كامل المستودع.

3. **فحص أخطاء الـ Console أثناء التصفح اليدوي:** صفر `console.error` غير معالَجة، صفر Promise rejection غير ملتقَطة، صفر أخطاء ربط قوالب Angular. **يجب تنفيذ هذه الخطوة دائماً قبل اعتبار أي إصلاح واجهة مكتملاً.**

4. **اختبارات الوحدة (Karma/Jasmine):**

   ```bash
   npx ng test --watch=false
   ```

   _القاعدة: 100% من الاختبارات ناجحة. العدد الحالي: 406/406._

5. **البناء الإنتاجي:**

   ```bash
   npx ng build --configuration=production
   ```

   _القاعدة: يجب أن ينتهي بـ `Application bundle generation complete` بدون أخطاء ولا تحذيرات._

6. **الدمج والنشر:**
   ```bash
   git checkout -b fix/<or>-feat/<scope>
   git add <ملفات محددة، وليس -A>
   git commit -m "..."
   git push -u origin <branch>
   git checkout develop && git merge --ff-only <branch>
   git push origin develop
   npx vercel --prod --yes
   ```

---

## 🔍 دليل الاختبار اليدوي — تدفقات الطالب (Student Flows)

### 1. قائمة الامتحانات (`/student/exams`)

تحقق من ظهور **كل الحالات الست** بشكل صحيح على بطاقة الامتحان، وأن كل حالة لها زر الإجراء الصحيح:

| الحالة                              | متى تظهر                             | زر الإجراء المتوقع                                    |
| ----------------------------------- | ------------------------------------ | ----------------------------------------------------- |
| `scheduled` (مجدول)                 | `startDate` في المستقبل              | معطّل، يعرض تاريخ البدء                               |
| `available` (متاح)                  | لا يوجد شرط آخر ينطبق                | **ابدأ الامتحان**                                     |
| `in-progress` (قيد التنفيذ)         | يوجد محاولة بحالة `InProgress`       | **استكمال المحاولة**                                  |
| `pending-grading` (بانتظار التصحيح) | `attemptStatus === 'PendingGrading'` | **عرض** (تظهر رسالة "قيد المراجعة" في صفحة النتيجة)   |
| `completed` (مكتمل)                 | `hasSubmitted` أو استُنفدت المحاولات | **عرض النتيجة**، و**إعادة المحاولة** إن تبقّت محاولات |
| `expired` (منتهي)                   | `endDate` مضى ولم يُسلَّم            | معطّل، لا يظهر زر فارغ                                |

- [ ] شارة **"بحاجة لمراجعة"** تظهر على البطاقة عندما يحمل أي attempt علم `needsTeacherReview`.
- [ ] عرض "المحاولة N من M" عندما `allowedAttempts > 1`.

### 2. أداء الامتحان (`/student/exams/:id/take`)

- [ ] **البار العلوي:** عنوان الامتحان والعداد التنازلي.
- [ ] **بطاقة السؤال:** إجابة MCQ باختيار واحد، وإجابة مقالية (Essay) بحقل نصي — تحقق من كلا النوعين على امتحان يحتوي عليهما معاً.
- [ ] **خريطة الأسئلة:** التنقل المباشر بين الأسئلة.
- [ ] **كارت المراقبة الأمنية:** تحذير عند تبديل التبويب؛ عند تجاوز حد المخالفات يُقفَل الامتحان ويُرسَل للخادم للتصحيح (لا تُكتَب نتيجة صفر محلياً).
- [ ] **الإرسال (Submit):** يُقبَل حتى مع استجابة `202` من الخادم، ثم ينتقل تلقائياً لتتبع التصحيح.
- [ ] **استكمال محاولة قيد التنفيذ:** أعد فتح امتحان بحالة `in-progress` وتأكد من استرجاع نفس الإجابات المحفوظة.

> ⚠️ **اختبار خاص — بدء الامتحان بشكل متكرر (Attempt/Start):** جرّب الضغط على "ابدأ الامتحان" على عدة امتحانات مختلفة بسرعة متتالية (أو افتح صفحة الامتحانات وأعد تحميلها عدة مرات)، خصوصاً بعد فترة عدم نشاط طويلة (أكثر من ساعة) بحيث يكون التوكن قريب من الانتهاء. كان فيه عطل متقطع هنا بسبب تعدد محاولات تجديد التوكن في نفس الوقت — تم إصلاحه بتاريخ 2026-08-24 بجعل كل الطلبات المتزامنة تشارك نفس محاولة التجديد. **لو حصل خروج مفاجئ من الحساب أو خطأ أثناء البدء، افتح الـ Console والـ Network tab وسجّل الخطأ بالتفصيل فوراً** — هذا أهم اختبار انحداري (regression) في هذه الدورة.

### 3. تتبع التصحيح والنتيجة (`/student/exams/:id/result?attemptId=...`)

> **ملاحظة مهمة:** بطاقة "المواضيع والمهارات الأضعف" (تحليل الذكاء الاصطناعي) أُزيلت من هذه الشاشة — تتبّع نقاط الضعف الآن حصرياً من شاشة **تقاريري** (انظر البند 4). لا تتوقع ظهورها هنا بعد الآن.

- [ ] **بطاقة النتيجة:** نسبة مئوية واحدة موثوقة (وليست مبنية على تخمين مقياس الدرجة)، شارة النجاح/الرسوب، تاريخ التسليم.
- [ ] **حالة "قيد التصحيح":** عند `isGradingPending`، تظهر أيقونة ⏳ بدل النسبة مع نص توضيحي، وزر **تحديث حالة النتيجة**.
- [ ] **مراجعة الأسئلة التفصيلية:** كل سؤال يعرض إجابة الطالب، الإجابة الصحيحة (عند توفرها)، وحالة "بانتظار مراجعة المعلم" للإجابات المقالية التي لم تُعتمَد بعد بدل تصنيفها كخاطئة.
- [ ] **زيارة محاولة سابقة تحديداً:** استخدم رابطاً يحمل `attemptId` محدد من عدة محاولات على نفس الامتحان، وتأكد من عرض تلك المحاولة بالذات وليس الأحدث دائماً.
- [ ] **تحديث فوري بعد تعديل المعلم لدرجة إجابة (Live Push):** لو المعلم عدّل درجة إجابة في محاولة الطالب وهو فاتح صفحة النتيجة، يجب أن تتحدث النتيجة تلقائياً (عبر SignalR) بدون الحاجة لتحديث الصفحة يدوياً — ميزة جديدة بتاريخ 2026-08-24.

### 4. تقاريري ودرجاتي (`/student/reports`)

- [ ] **قسم نقاط الضعف النشطة (Active):** شريط تقدّم بنسبة حقيقية من `GET /weaknesses/active` — بدون تلاعب أو مطابقة تقريبية مع درجات امتحانات أخرى. **تحقق أن النسبة منطقية وليست 0% للجميع** (كان فيه عطل هنا يُظهر 0% دايماً بسبب اسم حقل خاطئ — تم إصلاحه).
- [ ] **قسم المواضيع المتقَنة (Resolved):** شارة إنجاز وفرق التحسّن (delta) عند توفره من `GET /weaknesses/resolved`.
- [ ] **مراجعة الذكاء الاصطناعي لموضوع:** الضغط على "ابدأ المراجعة" يعرض توصية حقيقية من الخادم أو حالة خطأ صريحة مع إعادة محاولة — ولا يعرض أبداً نصاً افتراضياً ثابتاً عند فشل الطلب.
- [ ] **طلب امتحان تدريبي (Practice Exam):** يتتبّع التوليد عبر SignalR أو الاستطلاع (Polling) الفعلي، وليس عبر مطابقة عنوان الامتحان تخمينياً.
- [ ] **خريطة توزيع المهارات (Radar Chart) ومنحنى تطور الأداء:** يجب أن يعكسا بيانات الطالب الحقيقية (عدد المواد الفعلي، الأشهر الفعلية) — وليسا شكلاً ثابتاً لا يتغير. لو المواد أقل من 3 أو لا يوجد تاريخ تطور كافٍ، يظهر نص "لا توجد بيانات كافية" بدل رسم وهمي.

### 5. لوحة تحكم الطالب (`/student/dashboard`)

- [ ] **بطاقة "أهلاً بك":** لا يوجد نص "+X% هذا الشهر" ولا "أعلى من X% من الطلاب" — تم حذفهما بتاريخ 2026-08-24 لأنه لا يوجد مصدر بيانات حقيقي لهما من الخادم (كانا يظهران "+0%" و"أعلى من 0%" دائماً).
- [ ] **المتوسط التراكمي وسلسلة المذاكرة (Streak):** أرقام حقيقية من `overallAverage`/`currentStreak`، وليست قيم افتراضية ثابتة.
- [ ] **الامتحانات القادمة:** الضغط على أي بطاقة امتحان قادم ينقل لصفحة الامتحان الصحيحة (كان فيه عطل يخلي الرابط `undefined` — تم إصلاحه).
- [ ] **نقاط تحتاج تركيز (إن وُجدت):** تظهر مواضيع حقيقية من `pointsNeedingFocus`، وليس قسمًا فارغًا دائماً.

### 6. الإشعارات (جرس الإشعارات — أعلى أي صفحة)

- [ ] **جلسة جديدة (حساب لسه ما دخلش عليه قبل كده على هذا المتصفح):** يجب أن تبدأ قائمة الإشعارات **فارغة** مع رسالة "لا توجد إشعارات جديدة حالياً" — **وليس** إشعارَي "مرحباً بك" الثابتَين اللي كانا يظهروا لكل مستخدم قبل الإصلاح بتاريخ 2026-08-24.
- [ ] الإشعارات الحقيقية تظهر بعد التحميل من الخادم (`GET /Notifications`).
- [ ] تحديد إشعار كمقروء، تحديد الكل كمقروء، وحذف إشعار — كل هذه تُزامَن مع الخادم (تحقق من الـ Network tab).

---

## 🔍 دليل الاختبار اليدوي — تدفقات المعلم (Teacher Flows)

### 7. لوحة تحكم المعلم (`/teacher/dashboard`)

- [ ] **الشريط الجانبي:** يحتوي على رابط "مراجعات بانتظار الاعتماد" منفصل عن التنبيهات العامة.
- [ ] **بطاقة المراجعات المعلَّقة:** تحمّل من `GET /teachers/pending-reviews` وتُعرض كأكورديون فصل ← امتحان ← محاولة. **افحص الـ Console عند التحميل** — أي استجابة غير متوقعة الشكل يجب ألا تُسقِط الصفحة بالكامل.
- [ ] الضغط على صف مراجعة معلَّقة ينقل مباشرة إلى شاشة مراجعة تلك المحاولة.
- [ ] **متوسط الفصل (Class Average):** يظهر برقم متبوع بعلامة `%` دائماً — حتى بعد أول تحميل بيانات حقيقية (كانت علامة الـ% بتختفي بعد التحميل — تم إصلاحه).
- [ ] **قائمة "طلاب بحاجة متابعة":** تحقق أن تصنيف الخطورة (High/Medium) منطقي فعلاً مقابل الدرجة المعروضة — مش كل الطلاب "Medium" مهما كانت درجتهم منخفضة.
- [ ] **آخر التسليمات:** تحقق أن تصنيف "ممتاز/جيد/متوسط" منطقي فعلاً مقابل الدرجة المعروضة (كان فيه عطل تخمين مقياس بيخلي كل التسليمات تقريباً "ممتاز" مهما كانت الدرجة — تم إصلاحه).
- [ ] **الرسم البياني الأسبوعي (متوسط الأداء):** النسبة المعروضة أعلى الرسم منطقية (لو متوسط التسليمات 84%، لازم يظهر ~84% مش رقم غريب زي 17%).

### 8. توليد امتحان بالذكاء الاصطناعي (`/teacher/exams/generate`)

- [ ] **حصة الامتحانات المجانية:** لو عندك حصة مجانية متبقية هذا الشهر، يجب أن تظهر بطاقة خضراء "محاولات مجانية متاحة" مع شريط تقدّم ورقم صحيح (وليس "متبقي undefined من X محاولات") — كان فيه عطل يخلي الحصة المجانية تُتجاهَل دائماً حتى لو موجودة، ويحوّلك مباشرة لفحص الرصيد المدفوع. تم إصلاحه بتاريخ 2026-08-24.
- [ ] لو الحصة المجانية خالصة، تحقق أن فحص الرصيد المدفوع يعمل بشكل صحيح (رصيد كافٍ = مسموح بالتوليد، رصيد غير كافٍ = تنبيه واضح).

### 9. مراجعة إجابات محاولة (`/teacher/attempts/:attemptId/review`)

- [ ] تظهر الإجابات التي وضع الذكاء الاصطناعي عليها علم `needsTeacherReview` بشكل مميّز (نص السؤال، معيار التصحيح Rubric، إجابة الطالب، درجة الذكاء الاصطناعي).
- [ ] تعديل الدرجة عبر حقل رقمي محدود بـ `maxScore` ثم اعتماد التعديل يستدعي `PUT /attempts/{id}/answers/{answerId}/override`.
- [ ] بعد الاعتماد: يُعاد جلب النتائج تلقائياً وتظهر الدرجة الكلية المُعاد احتسابها من الخادم — **لا يُحسب أي مجموع محلياً على الواجهة**.
- [ ] **شارة "إجراء مطلوب" يجب أن تختفي فوراً بعد اعتماد التعديل** (كان فيه عطل يخلي الشارة تفضل ظاهرة حتى بعد الاعتماد لأن قراءة حالة `isFinalized` كانت من مكان خاطئ في البيانات — تم إصلاحه بتاريخ 2026-08-24). بدلها تظهر شارة "معتمدة" خضراء.
- [ ] السيناريو الكامل: درجة الذكاء الاصطناعي 0/10 ← تعديل المعلم إلى 10/10 ← بعد إعادة الجلب: الدرجة الكلية محدَّثة، شارة "معتمدة" ظاهرة (مش "إجراء مطلوب")، والموضوع المرتبط انتقل من نشط إلى محلول في `/student/reports` لنفس الطالب.

### 10. تقارير الطلاب (`/teacher/reports`)

- [ ] **المتوسط العام وأعلى درجة لطالب مُختار:** يجب أن تُطابق نفس الأرقام التي يراها الطالب نفسه في صفحة `/student/reports` الخاصة به — كان فيه عطل يخلي المعلم يشوف رقم مختلف عن الطالب لنفس البيانات بالظبط (منطق حساب مختلف في مكانين) — تم إصلاحه بتاريخ 2026-08-24.

---

## 🔍 دليل الاختبار اليدوي — المصادقة (Auth)

### 11. استعادة كلمة المرور بـ OTP (`/auth/forgot-password`)

- [ ] الخطوة الأولى: إدخال البريد الإلكتروني وطلب الرمز (`POST /auth/password-reset/request`).
- [ ] الخطوة الثانية: حقل الرمز يقبل **6 أرقام فقط** (رفض أي إدخال أقل أو غير رقمي).
- [ ] تأكيد كلمة المرور الجديدة عبر `POST /auth/password-reset/confirm`، مع رسائل خطأ واضحة (وليس Toast عام) عند فشل التحقق من الحقول.
- [ ] الدخول إلى `/auth/reset-password` مباشرة (المسار القديم) يجب أن يُعيد التوجيه تلقائياً إلى `/auth/forgot-password`.

### 12. الجلسة وتجديد التوكن (كل الصفحات)

- [ ] اترك التطبيق مفتوحاً بدون تفاعل لفترة طويلة (أو عدّل صلاحية التوكن محلياً للاختبار)، ثم قم بأي إجراء يستدعي عدة طلبات API متزامنة (مثل فتح صفحة امتحان). يجب أن يتم تجديد الجلسة بسلاسة **مرة واحدة فقط** بدون تسجيل خروج مفاجئ.
- [ ] لو حصل تسجيل خروج غير متوقع أثناء الاستخدام العادي، سجّل الوقت والصفحة والإجراء بالتفصيل — هذا يشير لمشكلة أعمق تحتاج فحص فوري.

---

## ⚠️ فحص إلزامي: أخطاء الـ Console

**يجب دائماً فتح أدوات المطوّر (DevTools Console) أثناء أي اختبار يدوي، لكل شاشة تم تعديلها، قبل تأكيد نجاح المهمة.** أخطاء الـ SignalR (`WebSocket connection failed`, `401` على `/hubs/*`) معروفة حالياً ومرتبطة بمشكلة بنية تحتية موثّقة (راجع Note 17 في `NOTES_FOR_BACKEND_DEVS.md`) — هذه تُسجَّل كـ `warn` وليست أخطاء توقف الصفحة، فتجاهلها عند تتبع أعطال أخرى. أي `console.error` أو استثناء غير ملتقَط (Unhandled Exception) يظهر مع أي شاشة أخرى هو عطل حقيقي ويجب إصلاحه فوراً قبل الدمج.

---

## 📚 مراجع إضافية

- `IMPLEMENTATION_WALKTHROUGH_2026-08-24.md` — تفاصيل كل عطل تم اكتشافه وإصلاحه في التدقيق الأخير، مع السبب الجذري وطريقة التحقق.
- `backend-report-latest.md` — الطلبات الحقيقية المتبقية للفريق الخلفي (Backend) بعد التدقيق.
- `NOTES_FOR_BACKEND_DEVS.md` — السجل الكامل لكل الملاحظات والطلبات الخلفية منذ بداية المشروع.
